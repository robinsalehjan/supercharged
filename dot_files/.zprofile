# Login shell environment variables (loaded once per login session)
# Note: CLICOLOR/LSCOLORS/LANG are set in .zshrc to ensure they apply in all shells
export SHELL=/usr/bin/zsh
export HOMEBREW_NO_ANALYTICS=1
export ASDF_FORCE_PREPEND=true
export SSH_KEY_PATH='$HOME/.ssh/'
