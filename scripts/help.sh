#!/bin/zsh

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "                           Scripts"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "Setup Commands:"
echo "  npm run setup          - Install dependencies, configure profiles, and validate"
echo "  npm run setup:profile  - Copy dotfiles and Claude Code configuration to \$HOME"
echo ""
echo "Backup & Restore Commands:"
echo "  npm run backup:claude          - Backup Claude Code config to repository"
echo "  npm run restore:claude         - Restore Claude Code config (only if repo is newer)"
echo "  npm run restore:claude -- --force - Force restore Claude Code config"
echo ""
echo "Update Commands:"
echo "  npm run update                 - Update all dependencies (includes Claude backup)"
echo "  npm run update:dry-run         - Preview updates without making changes"
echo "  npm run update:only -- <comp>  - Update only one component (brew, asdf, zsh, npm, pip)"
echo ""
echo "Development Commands:"
echo "  npm run lint                   - Run ShellCheck on all scripts"
echo "  npm test                       - Run all BATS tests"
echo "  bats tests/<suite>/*.bats      - Run a specific test suite (claude, utils, mac, update, setup)"
echo "  npm run test:watch             - Run tests continuously (requires watch)"
echo ""
echo "Other Commands:"
echo "  npm run validate       - Check if all tools are properly installed"
echo "  npm run restore        - Restore from the most recent backup"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
